package taskone;
import java.util.Scanner;

public class taskone {
    public static void main(String[] argstring){
        Scanner Scanner  = new Scanner(System.in);
        System.out.print("Enter avarage books read per mounth(v): ");
        int v =Scanner.nextInt();
        System.out.print("Enter avarage visitors per year(n):");
        int n = Scanner.nextInt();
        if (n <=0) {
            System.out.print("error,the number should not be less than 1");
        } else {
            double K = (v * 12) / n;

            System.out.printf("Avarage books read by per visitor per year (K) %2f%n", K);
        }
        Scanner.close();

        }
    }
